# =============================================================================
# DEREPUTE - DATA UPDATER CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env files with real private keys to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Smart Contract Configuration
# -----------------------------------------------------------------------------
# The deployed TorReputationStore contract address
# Get this from the deployment output (contracts/deployments/*.json)
CONTRACT_ADDRESS=your_deployed_contract_address_here

# Network name (optional, for logging/display purposes)
# Can be anything descriptive: ethereum, base, polygon, arbitrum, etc.
NETWORK=ethereum

# -----------------------------------------------------------------------------
# RPC Configuration (Chain-Agnostic)
# -----------------------------------------------------------------------------
# RPC URL for your target EVM chain
# Examples:
#   - Ethereum Mainnet: https://eth.llamarpc.com
#   - Base Mainnet: https://mainnet.base.org
#   - Polygon: https://polygon-rpc.com
#   - Arbitrum: https://arb1.arbitrum.io/rpc
#   - Optimism: https://mainnet.optimism.io
RPC_URL=https://eth.llamarpc.com

# -----------------------------------------------------------------------------
# Updater Wallet
# -----------------------------------------------------------------------------
# Private key for the wallet that will update relay data (without 0x prefix)
# This wallet must be authorized as an updater in the contract
# This wallet needs native currency for gas fees
# WARNING: Keep this secret! Never share or commit real keys!
#
# For local testing with Hardhat, you can use the test account #0:
# UPDATER_PRIVATE_KEY=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
UPDATER_PRIVATE_KEY=your_private_key_without_0x_prefix

# -----------------------------------------------------------------------------
# Onionoo API Configuration
# -----------------------------------------------------------------------------
# Base URL for the Tor Onionoo API
ONIONOO_BASE_URL=https://onionoo.torproject.org

# Maximum number of relays to fetch from the API
# Top relays by consensus weight will be selected
MAX_RELAYS_TO_FETCH=100

# -----------------------------------------------------------------------------
# Update Configuration
# -----------------------------------------------------------------------------
# Number of relays to include in each batch transaction
# Larger batches use more gas but require fewer transactions
# Recommended: 5-10 for Ethereum, 20-30 for Base/L2s
BATCH_SIZE=5

# Delay between batches in milliseconds (to avoid nonce conflicts)
# Higher values = more reliable, lower values = faster uploads
# Recommended: 3000 (3 seconds) for most chains, 1000-2000 for slower chains
BATCH_DELAY=3000

# Set to 'true' to test without sending transactions
# Set to 'false' to actually update the contract
DRY_RUN=false
